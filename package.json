{
  "name": "quickq",
  "version": "0.9.2",
  "main": "index.js",
  "description": "very fast in-memory multi-tenant work queue",
  "license": "Apache-2.0",
  "repository": {
    "url": "git://github.com/andrasq/node-quickq",
    "type": "git"
  },
  "keywords": [ "quick", "in-memory", "work", "job", "queue", "Andras" ],
  "dependencies": {
    "qlist": "0.13.1",
    "aflow": "0.10.1"
  },
  "devDependencies": {
    "qtimeit": "0.16.0"
  },
  "scripts": {
    "coverage": "nyc --reporter lcov --reporter text npm run test",
    "clean": "rm -rf .nyc_output coverage",
    "test": "qnit test/test-*",
    "coveralls": "nyc --reporter text-lcov npm run test | coveralls"
  }
}
